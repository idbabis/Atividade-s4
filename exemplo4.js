/* Imprima na tela os números pares existentes entre 0 e 100. */

let contador = 0;
    while(contador <= 100) {
        console.log(contador);
        contador = contador + 2;
    }
    console.log('Os numeros pares são esses');

